# agent-viz
agent_viz is a developer tool that provides real-time visualization of AI agent workflows directly in the browser. It captures agent actions, tool calls, LLM decisions, and outputs as they happen, rendering them as a live interactive graph using a lightweight web UI served over WebSockets. Developers can instrument their existing agent code with a 
